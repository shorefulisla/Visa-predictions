# Visa-predictions
##  tools you have to install 
 1. anaconda 
 2. vs code 
 3.git 
 
 ```bash
    git clone 

    ```bash
    conda create -n visa python=3.8 -y
    ```

    ```bash
    conda activate visa
    ```

    ```bash 
    pip install -r requirements.txt
    ```